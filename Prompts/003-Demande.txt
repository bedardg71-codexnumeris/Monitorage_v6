002-Demande

CONTEXTE : Application de monitorage pédagogique (index3.html). La sous-navigation principale est fonctionnelle. Je veux modifier la présentation visuelle.

OBJECTIF : Je veux que les boutons de la sous-navigation soient centrés dans la page plutôt qu'alignés à gauche. Voir capture.

ZONES AUTORISÉES : 
- Lignes de sous-navigation uniquement
- NE PAS toucher à la navigation principale

NOMS STABLES : sous-navigation, actif (INTOUCHABLES)

CODE ACTUEL  :
/* ===============================
           SOUS-NAVIGATION
           =============================== */

        .sous-navigation {
            background: var(--sous-nav-bg);
            padding: 10px 20px;
            display: flex;
            gap: 10px;
            border-bottom: 1px solid var(--bleu-leger);
            min-height: 50px;
            align-items: center;
        }

        .sous-navigation button {
            background: white;
            border: 1px solid var(--bleu-leger);
            color: var(--sous-nav-normal);
            padding: 8px 16px;
            font-size: 0.9rem;
            border-radius: 20px;
            cursor: pointer;
            transition: all 0.2s ease;
        }

        .sous-navigation button:hover {
            background: var(--bleu-pale);
            color: var(--sous-nav-hover);
        }

        .sous-navigation button.actif {
            background: var(--sous-nav-actif);
            color: white;
            border-color: var(--sous-nav-actif);
        }

        .sous-navigation.vide {
            justify-content: center;
            color: var(--bleu-leger);
            font-style: italic;
            font-size: 0.9rem;
        }


ATTENDU :
Boutons avec fond bleu (pas transparent)
Hover plus pâle
Actif avec bordure orange en bas

FORMAT : Patch minimal CSS uniquement